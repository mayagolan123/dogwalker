name: Run tests

on:
  workflow_dispatch:

jobs:
  tests:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout:latest 
  
      - name: Setup python version
        uses: ./.github/actions/set-python-version
          
